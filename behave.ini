# behave.ini

[behave]
# Define the path to your feature files
paths = ./features

# Define the path to your step definitions
steps = ./steps

# Specify the format for output reports (optional)
format = pretty

